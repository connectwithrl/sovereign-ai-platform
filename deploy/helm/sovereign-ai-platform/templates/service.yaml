apiVersion: v1
kind: Service
metadata:
  name: {{ include "sovereign-ai-platform.fullname" . }}
  labels:
    {{- include "sovereign-ai-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "sovereign-ai-platform.selectorLabels" . | nindent 4 }}
